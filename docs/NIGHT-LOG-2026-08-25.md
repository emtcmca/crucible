# Overnight log, 2026-08-24 into 2026-08-25

**Authorized by Eric**: run batches as long and hard as possible, tune and fix between
runs as issues surface, commit and push, delegate to agents. **Requirement: a clean log of
what was done and why, reviewable in the morning.** This is that log.

## Rollback, first, because it is the thing you would need in a hurry

**Baseline tag: `pre-batch-2026-08-24` at `89ea21f`.** Known good: pytest exit 0,
contract-check exit 0 across all five passes.

```bash
git log --oneline pre-batch-2026-08-24..main    # everything done overnight, in order
git revert <sha>                                # undo one change
git diff pre-batch-2026-08-24..main -- <path>   # what changed in one file
```

Every overnight change is **its own atomic commit on `main`** with the reasoning in the
message. Nothing is squashed and no history is rewritten, so any single change can be
reverted without disturbing the others. `git log pre-batch-2026-08-24..main` IS the
review list.

---

## Where the night started

`89ea21f` - items 3 and 4 landed as one contract move. INVALID verdicts now record why,
and ruling 37.1's approval-masked count finally has a producer. Full reasoning in that
commit message.

**Smoke, 2 runs, before committing the night to a config.** Purpose was to assert the new
fields land in a LIVE bundle rather than only in unit tests.

| Check | Result |
|---|---|
| run-01 exit code | 0 |
| INVALID episodes lacking a reason | **0 of 3** |
| reason distribution | `E_NO_EVENTS: 3` |
| C6 validation errors against the current reader | **0** |
| approval-masked count | **untested** - the run had zero breaches, so zero proposals |

Run 02 of the smoke was cut off by a foreground command timeout, not by a fault. The
batch runs detached for that reason.

**`E_NO_EVENTS: 3` is already the first result of the night.** Before tonight those three
episodes recorded nothing but `INVALID`, and the whole exclusion-ceiling question was
unanswerable because the two populations were indistinguishable. They are now labelled.

---

## Timeline

Entries are appended as work happens. Each names what changed, why, and the commit.
